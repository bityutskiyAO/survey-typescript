import { DefaultTheme } from 'styled-components'

const theme: DefaultTheme = {
    borderRadius: '8px',
    border: '3px solid #4135FF',
    colors: {
        default: 'black',
        primary: '#4135FF',
        secondary: 'white',
    },
}

export {
    theme
}
